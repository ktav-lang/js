# Local agent/dev scratch — per AGENTS.md §9, never commit or publish.
docs_local/

# Rust build artifacts
target/
Cargo.lock

# Node / JS
node_modules/
dist/
*.tsbuildinfo

# wasm-pack scratch
pkg/

# napi-rs CLI staging for release — `.node` binaries live in
# per-platform optionalDependencies that are published separately.
# Their package.json lives in git; the native binary is dropped in
# by CI via `napi artifacts` right before `npm publish`.
npm/*/*.node

# IDE / editor metadata
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
