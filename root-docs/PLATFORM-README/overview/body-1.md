>>>>> lang=en
# `{{PACKAGE_NAME}}` platform binaries

This package contains the prebuilt N-API and C ABI libraries for
`{{TARGET_TRIPLE}}`. It is an optional dependency of [`@ktav-lang/ktav`](https://www.npmjs.com/package/@ktav-lang/ktav)
and is normally installed automatically; install the main package to
use the JavaScript API.
>>>>> lang=ru
# Платформенные бинарники `{{PACKAGE_NAME}}`

Этот пакет содержит готовые библиотеки N-API и C ABI для
`{{TARGET_TRIPLE}}`. Он является необязательной зависимостью
[`@ktav-lang/ktav`](https://www.npmjs.com/package/@ktav-lang/ktav)
и обычно устанавливается автоматически. Для JavaScript API установите
основной пакет.
>>>>> lang=zh
# `{{PACKAGE_NAME}}` 平台二进制包

此包包含用于 `{{TARGET_TRIPLE}}` 的预构建 N-API 和 C ABI 库。它是
[`@ktav-lang/ktav`](https://www.npmjs.com/package/@ktav-lang/ktav)
的可选依赖，通常会自动安装；使用 JavaScript API 时请安装主包。
